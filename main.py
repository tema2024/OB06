class Герой:
    def __init__(self, имя):
        self.имя = имя
        self.здоровье = 100
        self.сила_удара = 20

    def атаковать(self, другой):
        другой.здоровье -= self.сила_удара

    def живой(self):
        return self.здоровье > 0


class Игра:
    def __init__(self, имя_игрока, имя_компьютера):
        self.игрок = Герой(имя_игрока)
        self.компьютер = Герой(имя_компьютера)

    def начать(self):
        ход = 0  # Игрок начинает
        while self.игрок.живой() and self.компьютер.живой():
            if ход % 2 == 0:
                self.игрок.атаковать(self.компьютер)
                print(f"{self.игрок.имя} атакует! У {self.компьютер.имя} осталось {self.компьютер.здоровье} здоровья.")
            else:
                self.компьютер.атаковать(self.игрок)
                print(f"{self.компьютер.имя} атакует! У {self.игрок.имя} осталось {self.игрок.здоровье} здоровья.")
            ход += 1

        if self.игрок.живой():
            print(f"{self.игрок.имя} побеждает!")
        else:
            print(f"{self.компьютер.имя} побеждает!")


# Создаем и запускаем игру
игра = Игра("Игрок", "Компьютер")
игра.начать()
